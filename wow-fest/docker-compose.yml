services:
  web:
    build: .
    ports:
      - "5002:5000"
    volumes:
      - ./data:/app/data
      # для разработки: раскомментируйте, чтобы видеть изменения в шаблонах/статике без пересборки
      # - ./templates:/app/templates
      # - ./static:/app/static
    environment:
      - WOWFEST_SHEETS_WEBHOOK_URL=${WOWFEST_SHEETS_WEBHOOK_URL:-}
      - WOWFEST_SPREADSHEET_ID=${WOWFEST_SPREADSHEET_ID:-}
      - WOWFEST_GA4_ID=${WOWFEST_GA4_ID:-}
      - WOWFEST_FB_PIXEL_ID=${WOWFEST_FB_PIXEL_ID:-}
      - WOWFEST_SMTP_HOST=${WOWFEST_SMTP_HOST:-}
      - WOWFEST_SMTP_PORT=${WOWFEST_SMTP_PORT:-465}
      - WOWFEST_SMTP_USER=${WOWFEST_SMTP_USER:-}
      - WOWFEST_SMTP_PASS=${WOWFEST_SMTP_PASS:-}
      - WOWFEST_SMTP_FROM=${WOWFEST_SMTP_FROM:-}
    restart: unless-stopped
